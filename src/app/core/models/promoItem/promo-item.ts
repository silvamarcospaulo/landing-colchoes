export class PromoItem {
    constructor(
        public label: string,
        public description: string,
        public icon: string
    ) {
        this.label = label;
        this.description = description;
        this.icon = icon;
    }
}